# Deez Project Manager — env placeholders
# Mesh sync uses a GitHub PAT with `gist` scope (Settings → Mesh network).
# Desktop stores it in the app credential store — do not put tokens here.
# GITHUB_TOKEN=