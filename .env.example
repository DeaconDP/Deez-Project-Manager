# Deez Project Manager — env placeholders
# Mesh sync + private repo import use a GitHub PAT with `gist` + `repo` scopes
# (Settings → Mesh network). Desktop stores it in the app credential store —
# do not put tokens here.
# GITHUB_TOKEN=